const getPineconeClient = () => {
  // Placeholder for future Pinecone integration.
  // Keep this adapter so RAG migration stays localized to config/services.
  return null;
};

module.exports = {
  getPineconeClient,
};
